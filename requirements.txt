#
# Backend "core" dependencies only.
#
# Why: Homebrew Python on macOS is "externally managed" (PEP 668) and requires a venv.
# Also, the full (un-pinned) AI provider list can trigger pip "resolution-too-deep".
#
# If you want voice AI providers (Whisper/Swara/Google/Azure/etc), install one of:
# - requirements-ai-local.txt
# - requirements-ai-cloud.txt
#

fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.22
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.2.1

# Utilities / HTTP
httpx==0.28.1
aiofiles==25.1.0
requests==2.32.5
gTTS==2.5.4

# Commerce / Shopify
shopify-python-api==1.0.1
gql[all]==4.0.0

# AI Providers (AssemblyAI for STT)
assemblyai==0.48.4
